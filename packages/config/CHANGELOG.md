# Changelog

## 1.0.0

- Initial release of `@xtaskjs/config`.
- Typed environment configuration with Zod schema inference.
- Fail-fast startup validation via `initializeConfigIntegration`.
- `.env` loading with optional prefix mapping.
- DI integration with `ConfigService` and injection decorators.
