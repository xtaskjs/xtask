import { ApplicationLifeCycle, Container } from "@xtaskjs/core";
import { DataSource, ObjectLiteral, ObjectType, Repository } from "typeorm";
import { CommandBus, EventBus, QueryBus } from "./bus";
import { IIdempotencyStore } from "./types";
export declare class CqrsLifecycleManager {
    private container?;
    private lifecycle?;
    private stoppingRegistered;
    private initialized;
    private idempotencyStore;
    private readonly commandHandlers;
    private readonly queryHandlers;
    private readonly eventHandlers;
    private readonly processManagers;
    private readonly projectionRebuilders;
    private readonly idempotentCommands;
    private readonly inFlightIdempotentCommands;
    private readonly commandBus;
    private readonly queryBus;
    private readonly eventBus;
    initialize(container?: Container, lifecycle?: ApplicationLifeCycle): Promise<void>;
    destroy(): Promise<void>;
    isInitialized(): boolean;
    getContainer(): Container | undefined;
    getLifecycle(): ApplicationLifeCycle | undefined;
    getCommandBus(): CommandBus;
    getQueryBus(): QueryBus;
    getEventBus(): EventBus;
    getIdempotencyStore(): IIdempotencyStore;
    getReadDataSource(): DataSource;
    getWriteDataSource(): DataSource;
    getReadRepository<T extends ObjectLiteral>(entity: ObjectType<T>): Repository<T>;
    getWriteRepository<T extends ObjectLiteral>(entity: ObjectType<T>): Repository<T>;
    listProjectionRebuilders(): string[];
    rebuildProjection(name: string): Promise<void>;
    rebuildAllProjections(): Promise<string[]>;
    executeCommand<TResult = any>(command: any): Promise<TResult>;
    executeQuery<TResult = any>(query: any): Promise<TResult>;
    publishEvent(event: any): Promise<void>;
    private registerHandlers;
    private registerCoreContainerBindings;
    private registerHandlerContainerBindings;
    private resolveIdempotencyKey;
    private registerRepositoryAliases;
    private getConfiguredDataSource;
}
export declare const initializeCqrsIntegration: (container?: Container, lifecycle?: ApplicationLifeCycle) => Promise<void>;
export declare const shutdownCqrsIntegration: () => Promise<void>;
export declare const getCqrsLifecycleManager: () => CqrsLifecycleManager;
export declare const resetCqrsIntegration: () => Promise<void>;
