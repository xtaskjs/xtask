# 01-new_app

Node HTTP sample application using `@xtaskjs/core`.

## Run

```bash
npm install
npm start
```

From this folder: `samples/01-new_app`.

## Test URLs

- Health endpoint (JSON):
  - http://127.0.0.1:3000/health

## Notes

- Uses the default `node-http` adapter.
