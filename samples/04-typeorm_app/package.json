{
  "name": "04-typeorm_app",
  "version": "1.0.0",
  "description": "xTaskJS + TypeORM + SQLite sample",
  "main": "index.js",
  "scripts": {
    "start": "ts-node -r tsconfig-paths/register ./src/app.ts",
    "build": "npm run clean; tsc",
    "clean": "rm -rf dist",
    "start:parallel": "XTASK_SCAN_WORKERS=auto ts-node -r tsconfig-paths/register ./src/app.ts",
    "test": "ts-node -r tsconfig-paths/register ./src/app.spec.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@xtaskjs/common": "workspace:*",
    "@xtaskjs/core": "workspace:*",
    "@xtaskjs/fastify-http": "workspace:*",
    "@xtaskjs/typeorm": "workspace:*",
    "fastify": "^5.2.1",
    "reflect-metadata": "^0.2.2",
    "sqlite3": "^5.1.7",
    "typeorm": "^0.3.28"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.2",
    "@xtaskjs/testing": "workspace:*"
  }
}
