{
  "name": "22-event_source_cqrs_app",
  "version": "1.0.0",
  "description": "xTaskJS sample where event-source feeds CQRS projections and queries.",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run build --prefix ../../packages/event-source && npm run build --prefix ../../packages/cqrs",
    "start": "ts-node -r tsconfig-paths/register ./src/app.ts",
    "build": "npm run clean; tsc",
    "clean": "rm -rf dist",
    "start:parallel": "XTASK_SCAN_WORKERS=auto ts-node -r tsconfig-paths/register ./src/app.ts",
    "test": "ts-node -r tsconfig-paths/register ./src/app.spec.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@xtaskjs/common": "workspace:*",
    "@xtaskjs/core": "workspace:*",
    "@xtaskjs/cqrs": "workspace:*",
    "@xtaskjs/event-source": "workspace:*",
    "@xtaskjs/typeorm": "workspace:*",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "reflect-metadata": "^0.2.2",
    "sqlite3": "^5.1.7",
    "ts-morph": "^27.0.0",
    "typeorm": "^0.3.28"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.2",
    "@xtaskjs/testing": "workspace:*"
  }
}
